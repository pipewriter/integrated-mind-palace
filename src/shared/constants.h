// ================================================================
// Shared constants — values that must match between client and server
// ================================================================
#pragma once

#include <cstdint>

// World grid
constexpr int   GRID_SIZE   = 512;
constexpr float GRID_SCALE  = 2.0f;

// Trail system
constexpr int   TRAIL_TEX_SIZE       = 2048;
constexpr float TRAIL_BRUSH_RADIUS   = 1.8f;
constexpr float TRAIL_PAINT_PER_UNIT = 0.10f;
